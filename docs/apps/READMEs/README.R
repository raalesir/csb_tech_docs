=========================================
=            Running R at csb           =
=========================================

To load R just use the module program:

    module load r/3.4.1

Then you can test with say:

    Rscript -e "4*atan(1)"

Which should result in a precise pi
